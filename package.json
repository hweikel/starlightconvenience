{
  "name": "starlight-convenience",
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf _site && eleventy",
    "serve": "rm -rf _site && eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
