# starlightconvenience

Personal site built with [11ty](https://www.11ty.dev/).

## Local dev

```
npx @11ty/eleventy --serve
```

(`npm run serve` doesn't work because eleventy isn't installed locally.)
